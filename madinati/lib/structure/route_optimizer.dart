import 'package:flutter/material.dart';

class RouteOptimizer extends StatelessWidget {
  const RouteOptimizer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Route Optimizer Screen'),
      ),
    );
  }
}
